<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI-Powered Intelligent File Assistant - Sindika</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Inter', sans-serif; 
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
        
        @keyframes pulse-glow {
            0%, 100% { box-shadow: 0 0 20px rgba(59, 130, 246, 0.3); }
            50% { box-shadow: 0 0 40px rgba(59, 130, 246, 0.6); }
        }
        
        @keyframes slide-in-left {
            from { opacity: 0; transform: translateX(-30px); }
            to { opacity: 1; transform: translateX(0); }
        }
        
        @keyframes slide-in-right {
            from { opacity: 0; transform: translateX(30px); }
            to { opacity: 1; transform: translateX(0); }
        }
        
        @keyframes typing {
            from { width: 0; }
            to { width: 100%; }
        }
        
        .animate-float {
            animation: float 3s ease-in-out infinite;
        }
        
        .animate-pulse-glow {
            animation: pulse-glow 2s ease-in-out infinite;
        }
        
        .chat-message-user {
            animation: slide-in-right 0.5s ease-out forwards;
        }
        
        .chat-message-ai {
            animation: slide-in-left 0.5s ease-out forwards;
        }
        
        .typing-indicator {
            display: inline-block;
            animation: typing 2s steps(40) infinite;
            overflow: hidden;
            white-space: nowrap;
            border-right: 2px solid #3b82f6;
        }
        
        .file-item {
            transition: all 0.3s ease;
        }
        
        .file-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }
        
        .gradient-text {
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-slate-50 via-blue-50 to-purple-50 text-slate-800">

    <!-- Navbar Placeholder -->
    <%- include('partials/_navbar') %>

    <!-- Background Effects -->
    <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10">
        <div class="absolute top-0 left-0 -translate-x-1/3 w-[50rem] h-[50rem] bg-blue-300/30 rounded-full blur-3xl animate-float"></div>
        <div class="absolute bottom-0 right-0 translate-x-1/3 w-[50rem] h-[50rem] bg-purple-300/30 rounded-full blur-3xl animate-float" style="animation-delay: 1s;"></div>
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[40rem] h-[40rem] bg-pink-200/20 rounded-full blur-3xl animate-float" style="animation-delay: 2s;"></div>
    </div>

    <main class="w-full min-h-screen pt-32 pb-16 px-4">
        <section class="container mx-auto max-w-7xl">
            
            <!-- Hero Section -->
            <div class="text-center mb-16">
                <h1 class="text-6xl font-black text-slate-900 tracking-tight mb-6">
                    AI-Powered <span class="gradient-text">Intelligent</span><br/>File Assistant
                </h1>
                <p class="mt-4 max-w-2xl mx-auto text-xl text-slate-600 leading-relaxed">
                    Asisten AI yang memahami konteks gereja Anda. Temukan dokumen dengan bahasa natural, dapatkan ringkasan instan, dan ekstrak informasi penting hanya dengan bertanya.
                </p>
            </div>

            <!-- Interactive Demo Section -->
            <div class="mb-20 w-full max-w-6xl mx-auto">
                <div class="bg-white/70 backdrop-blur-xl border border-white/50 rounded-3xl shadow-2xl overflow-hidden animate-pulse-glow">
                    <div class="grid grid-cols-1 md:grid-cols-2 min-h-[600px]">
                        
                        <!-- File Explorer Side -->
                        <div class="bg-gradient-to-br from-slate-50 to-slate-100 p-6 border-r border-slate-200">
                            <div class="flex items-center justify-between mb-6">
                                <h3 class="text-lg font-bold text-slate-800 flex items-center gap-2">
                                    <span class="text-2xl">📁</span> Dokumen Gereja
                                </h3>
                                <button class="px-3 py-1 bg-blue-500 text-white text-sm rounded-lg hover:bg-blue-600 transition">Upload</button>
                            </div>
                            
                            <div class="space-y-3" id="fileList">
                                <div class="file-item p-4 bg-white rounded-xl border border-slate-200 cursor-pointer">
                                    <div class="flex items-start gap-3">
                                        <span class="text-3xl">📄</span>
                                        <div class="flex-1">
                                            <h4 class="font-semibold text-slate-800 text-sm">Notulensi Rapat Keuangan</h4>
                                            <p class="text-xs text-slate-500 mt-1">15 September 2024 • 2.3 MB</p>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="file-item p-4 bg-white rounded-xl border border-slate-200 cursor-pointer">
                                    <div class="flex items-start gap-3">
                                        <span class="text-3xl">📊</span>
                                        <div class="flex-1">
                                            <h4 class="font-semibold text-slate-800 text-sm">Laporan Keuangan Q3 2024</h4>
                                            <p class="text-xs text-slate-500 mt-1">30 September 2024 • 5.7 MB</p>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="file-item p-4 bg-white rounded-xl border border-slate-200 cursor-pointer">
                                    <div class="flex items-start gap-3">
                                        <span class="text-3xl">🏗️</span>
                                        <div class="flex-1">
                                            <h4 class="font-semibold text-slate-800 text-sm">Proposal Renovasi Gedung</h4>
                                            <p class="text-xs text-slate-500 mt-1">5 Agustus 2024 • 8.1 MB</p>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="file-item p-4 bg-white rounded-xl border border-slate-200 cursor-pointer">
                                    <div class="flex items-start gap-3">
                                        <span class="text-3xl">📋</span>
                                        <div class="flex-1">
                                            <h4 class="font-semibold text-slate-800 text-sm">Agenda Kebaktian Oktober</h4>
                                            <p class="text-xs text-slate-500 mt-1">1 Oktober 2024 • 1.2 MB</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Chatbot Side -->
                        <div class="bg-white p-6 flex flex-col">
                            <div class="flex items-center gap-3 mb-6 pb-4 border-b border-slate-200">
                                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-white font-bold">AI</div>
                                <div>
                                    <h3 class="font-bold text-slate-800">AI Assistant</h3>
                                    <p class="text-xs text-green-600 flex items-center gap-1">
                                        <span class="w-2 h-2 bg-green-500 rounded-full"></span> Online
                                    </p>
                                </div>
                            </div>
                            
                            <div class="flex-1 overflow-y-auto space-y-4 mb-4" id="chatMessages">
                                <div class="chat-message-ai flex gap-3">
                                    <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-white text-sm flex-shrink-0">AI</div>
                                    <div class="bg-slate-100 rounded-2xl rounded-tl-none px-4 py-3 max-w-[80%]">
                                        <p class="text-sm text-slate-700">Halo! Saya siap membantu Anda mencari dan menganalisis dokumen. Ada yang bisa saya bantu?</p>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="flex gap-2">
                                <input type="text" id="chatInput" placeholder="Tanya tentang dokumen Anda..." class="flex-1 px-4 py-3 bg-slate-100 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm">
                                <button onclick="sendMessage()" class="px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-600 text-white rounded-xl hover:shadow-lg transition font-semibold">
                                    Kirim
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Features Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-left">
                <div class="group p-8 bg-white/70 backdrop-blur-xl border border-white/50 rounded-3xl shadow-xl hover:shadow-2xl transition-all duration-300 hover:-translate-y-2">
                    <div class="text-5xl mb-4 group-hover:scale-110 transition-transform">🔍</div>
                    <h3 class="text-2xl font-bold text-slate-800 mb-3">Pencarian Cerdas</h3>
                    <p class="text-slate-600 leading-relaxed">Jangan buang waktu mencari berdasarkan nama file. Cukup ajukan pertanyaan seperti "cari proposal renovasi gedung dari tahun lalu" dan biarkan AI menemukannya untuk Anda.</p>
                </div>

                <div class="group p-8 bg-white/70 backdrop-blur-xl border border-white/50 rounded-3xl shadow-xl hover:shadow-2xl transition-all duration-300 hover:-translate-y-2">
                    <div class="text-5xl mb-4 group-hover:scale-110 transition-transform">📝</div>
                    <h3 class="text-2xl font-bold text-slate-800 mb-3">Ringkasan Otomatis</h3>
                    <p class="text-slate-600 leading-relaxed">Tidak perlu membaca dokumen 20 halaman. Minta AI untuk memberikan ringkasan poin-poin penting dari notulensi rapat atau laporan bulanan dalam hitungan detik.</p>
                </div>

                <div class="group p-8 bg-white/70 backdrop-blur-xl border border-white/50 rounded-3xl shadow-xl hover:shadow-2xl transition-all duration-300 hover:-translate-y-2">
                    <div class="text-5xl mb-4 group-hover:scale-110 transition-transform">💡</div>
                    <h3 class="text-2xl font-bold text-slate-800 mb-3">Rekomendasi Dokumen</h3>
                    <p class="text-slate-600 leading-relaxed">Saat Anda mengerjakan sebuah proyek, AI akan secara proaktif merekomendasikan dokumen-dokumen relevan dari arsip Anda yang mungkin berguna.</p>
                </div>
            </div>
        </section>
    </main>

     <%- include('partials/_footer') %> 

    <script>
        const chatMessages = document.getElementById('chatMessages');
        const chatInput = document.getElementById('chatInput');
        let messageCount = 0;

        // Predefined conversation flow
        const conversation = [
            { type: 'user', text: 'Cari notulensi rapat keuangan terakhir' },
            { type: 'ai', text: 'Saya menemukan "Notulensi Rapat Keuangan" dari 15 September 2024. Dokumen ini membahas alokasi dana untuk renovasi dan anggaran kegiatan Q4. Ingin saya buka atau ringkas?' },
            { type: 'user', text: 'Berikan ringkasannya' },
            { type: 'ai', text: 'Berikut ringkasan notulensi:\n\n• Persetujuan anggaran renovasi Rp 150 juta\n• Alokasi dana kegiatan pemuda Rp 25 juta\n• Target pengumpulan persembahan Q4: Rp 200 juta\n• Rencana audit keuangan bulan November\n\nAda yang ingin Anda tanyakan lebih lanjut?' }
        ];

        function sendMessage() {
            const message = chatInput.value.trim();
            if (!message) return;

            addMessage('user', message);
            chatInput.value = '';

            setTimeout(() => {
                if (messageCount < conversation.length && conversation[messageCount].type === 'ai') {
                    addMessage('ai', conversation[messageCount].text);
                    messageCount++;
                }
            }, 1000);
        }

        function addMessage(type, text) {
            const messageDiv = document.createElement('div');
            messageDiv.className = `chat-message-${type} flex gap-3 ${type === 'user' ? 'justify-end' : ''}`;
            
            if (type === 'ai') {
                messageDiv.innerHTML = `
                    <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-white text-sm flex-shrink-0">AI</div>
                    <div class="bg-slate-100 rounded-2xl rounded-tl-none px-4 py-3 max-w-[80%]">
                        <p class="text-sm text-slate-700 whitespace-pre-line">${text}</p>
                    </div>
                `;
            } else {
                messageDiv.innerHTML = `
                    <div class="bg-gradient-to-r from-blue-500 to-purple-600 text-white rounded-2xl rounded-tr-none px-4 py-3 max-w-[80%]">
                        <p class="text-sm whitespace-pre-line">${text}</p>
                    </div>
                `;
            }
            
            chatMessages.appendChild(messageDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        // Auto-play demo conversation
        let autoPlayIndex = 0;
        function autoPlayDemo() {
            if (autoPlayIndex < conversation.length) {
                const msg = conversation[autoPlayIndex];
                if (msg.type === 'user') {
                    chatInput.value = msg.text;
                    setTimeout(() => {
                        sendMessage();
                        autoPlayIndex++;
                        setTimeout(autoPlayDemo, 2000);
                    }, 1000);
                } else {
                    autoPlayIndex++;
                    setTimeout(autoPlayDemo, 2000);
                }
            } else {
                // Reset after completion
                setTimeout(() => {
                    chatMessages.innerHTML = `
                        <div class="chat-message-ai flex gap-3">
                            <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-white text-sm flex-shrink-0">AI</div>
                            <div class="bg-slate-100 rounded-2xl rounded-tl-none px-4 py-3 max-w-[80%]">
                                <p class="text-sm text-slate-700">Halo! Saya siap membantu Anda mencari dan menganalisis dokumen. Ada yang bisa saya bantu?</p>
                            </div>
                        </div>
                    `;
                    autoPlayIndex = 0;
                    messageCount = 0;
                    setTimeout(autoPlayDemo, 3000);
                }, 5000);
            }
        }

        // Start auto-play after 2 seconds
        setTimeout(autoPlayDemo, 2000);

        // Allow Enter key to send message
        chatInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                sendMessage();
            }
        });

        // File highlight animation
        const fileItems = document.querySelectorAll('.file-item');
        let currentHighlight = 0;

        setInterval(() => {
            fileItems.forEach(item => item.classList.remove('ring-2', 'ring-blue-500'));
            fileItems[currentHighlight].classList.add('ring-2', 'ring-blue-500');
            currentHighlight = (currentHighlight + 1) % fileItems.length;
        }, 3000);
    </script>

</body>
</html>